{
  "name": "gateway-sim",
  "private": true,
  "version": "1.0.0",
  "description": "Gateway ảo đa nút + trang quản lý — bắn status + traffic (data AI) cho các nút giao, nhận lệnh từ server.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongodb": "^6.3.0",
    "mqtt": "^5.3.5"
  }
}
